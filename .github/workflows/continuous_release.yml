name: Continuous Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [master]
  workflow_dispatch:
    inputs:
      release_impact:
        description: Release impact for unreleased changes already on master
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  actions: read
  contents: read
  issues: read
  pull-requests: read

jobs:
  release:
    uses: AtlassianPS/AtlassianPS.Standards/.github/workflows/module_release.yml@347ded1033c302cf6dbbd8c614aefe2477121ab0 # v0.4.0
    with:
      module-name: AtlassianPS.Configuration
      release-impact: ${{ inputs.release_impact }}
    secrets: inherit
